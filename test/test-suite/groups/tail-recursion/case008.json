{
    "expr": "($f := function($s, $x)<an:s> { $x > 0 ? $f([$s, $s], $x-1) : $s};  $f('a', 2)  )",
    "dataset": null,
    "timelimit": 1000,
    "depth": 500,
    "bindings": {},
    "result": ["a","a","a","a"]
}